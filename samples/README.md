# Sample artifacts (sanitized)

Redacted examples of what the agents actually read and write, so the architecture has texture without exposing any personal data. Every name, number, company, and id here is a placeholder. These are illustrations, not live files.

- `decision-canvas.md` — a triage canvas row format (the two-way edit channel).
- `morning-brief.md` — the ranked daily view with a cross-domain top three.
- `end-of-day-scorecard.md` — the plan-vs-actual nightly grade.
- `changelog.md` — the single append-only audit log.
- `digest-sms.txt` — a per-channel digest text in the house style.
