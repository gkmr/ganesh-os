A pill row of 2-4 options; active one gets the violet gradient. Use for view/tier/domain switches.

```jsx
<SegmentedControl
  options={[{value:"today",label:"Today"},{value:"week",label:"This week"},{value:"later",label:"Later"}]}
  value={tier} onChange={setTier}
/>
```
