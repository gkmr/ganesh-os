Pill toggle, violet when on. Use for binary settings like auto-park or travel-day detection.

```jsx
<Switch checked={on} onChange={e => setOn(e.target.checked)} label="Auto-park overdue items forward" />
```
