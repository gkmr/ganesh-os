Custom dark checkbox, violet when checked. Use for opt-in lists and confirm-gates.

```jsx
<Checkbox checked={c} onChange={e => setC(e.target.checked)} label="Exclude recurring rituals from the count" />
```
