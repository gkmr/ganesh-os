The default dark surface — hairline border, 18px radius, soft shadow. Add a domain accent edge and `interactive` for the hover lift + violet glow. Ships with `CardEyebrow` (mono uppercase label) and `CardOwner` (the "owns: …" pill).

```jsx
<Card accent="health" interactive>
  <CardEyebrow accent="health">Health</CardEyebrow>
  <h3 style={{ color: "var(--ink)", margin: "9px 0 5px" }}>Strength</h3>
  <p style={{ margin: 0, color: "var(--muted)", fontSize: 13 }}>
    The 6 p.m. lift is a protected block. No sweep can bump it.
  </p>
  <CardOwner style={{ marginTop: 11 }}>held: streak-aware</CardOwner>
</Card>
```

`accent` takes a domain (`work | health | people | growth | brand`) or any CSS color; `accentSide="left"` for catalog/agent rows. `gradientSurface` uses the subtle top-down surface fill.
