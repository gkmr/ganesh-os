Hover/focus tooltip bubble around a single trigger. Use for handle legends, field-owner hints.

```jsx
<Tooltip content="Owned by the sweeps. Read-only to every other agent.">
  <span style={{ borderBottom: "1px dotted var(--faint)" }}>due_date</span>
</Tooltip>
```
