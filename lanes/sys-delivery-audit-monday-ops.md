# sys · delivery-audit (Monday, ops)

- id: trig_01KSpqg57isFNr2aLa93Vy6V
- cron (UTC): 30 12 * * 1
- model: claude-sonnet-4-5
- enabled: True
- connectors: Atlassian, Google_Drive, TickTick, Slack, Krisp_AI, Gmail, Google_Calendar, Granola, Claude_Code_Remote, Figma, Excalidraw, PayPal, Mermaid_Chart

## Prompt

[Sys] Weekly delivery-health audit, cloud. Fires Mondays 12:30 UTC (8:30 AM EDT). READ-ONLY everywhere except its own state file and its own delivery. Purpose: catch silent delivery degradation across the fleet before GK notices it.

AUDIT (work the checks in order; every check is PASS/FAIL with evidence):
1. TRIGGER FIRES: call list_triggers (oversized payload saved to a file — query with jq/python, never paste whole). For each enabled trigger, compare last_fired_at against its cron cadence over the past 7 days; last fire >~1.5 cadence intervals old = MISS.
2. OUTBOX DRAINAGE: list Drive Claude-Fleet-State/telegram-outbox/ (id 184JoMq2Kg2ZsKNLDHLbOswag9XpKrur8). Files older than 24h without .delivered markers = drainer down; count + name the oldest. (v7.4 archive/ subfolder holds delivered history; exclude it from the pending scan. NOTE: the relay main() trigger runs every 15 min since GK's 8/4 M20 stretch — drainage latency up to ~15 min is normal, not degradation.) DEAD-LETTER VISIBILITY (GK 2026-08-04): also list every .delivered marker from the past 7 days whose content begins "DEAD-LETTER" (content guard or 3-strikes) — report each in the ops scorecard as name + refusal reason, info-only (the relay never deletes content; the original file stays). A dead-letter whose reason implicates a writer task = name the task. QUARANTINE TRIPWIRE: RETIRED — GK executed M19 on 2026-08-04; the folder id (17K4JYE0sNLKHwARAVXjLp3hMx2mcHD11) no longer resolves, verified same day. Do not list or probe it; this line is the record.
3. RELAY STATUS + DIRECT-PATH CANARY (VERDICT-BY-SENT-LOG since 2026-08-10 — after GK's v7.4.4 redeploy the /exec endpoint returns HTTP 405 error pages on POSTs that actually DELIVER; HTTP codes are no longer evidence in either direction): read Claude-Fleet-State/telegram-relay-config.json; note the sent-log's last entry timestamp; POST ONE test line {secret, chat_id, text:"🩺 [sys/delivery-audit] weekly probe", "ops": true} (OPS group, not main; POST once, follow any 302 as GET, never re-POST to the redirect target). Wait 60-90s, re-read telegram-sent-log.jsonl: a NEW ops entry matching the probe = PASS regardless of HTTP code; {ok:true} or {ok:true, deduped:true} in the response body also = PASS (the v7.4.4 near-dup guard suppresses same-tag repeats within 15 min — never send the probe twice, and never treat deduped:true as a failure). FAIL only when the sent-log shows no new matching entry — that = direct-path regression: say so in the main-chat one-liner, note the outbox still carries traffic (degraded, not dark). Also read the last 7 daily fallback-sweep run markers (relay-canary field) and report direct-path uptime out of 7.
4. REPLY LANE: telegram-inbox.jsonl freshness (growth in 7 days?) + telegram-replies-state.json offset advancement. Inbox grew but offset frozen = SEVERE. (Since relay v7.4.3 the inbox carries chat:"main"|"ops" tags — count both, report the split.) §14 COMPLIANCE (2026-08-10): the state file's routing records must show applied:true or routed:<lane> for every consumed line — any "seen, not applied"-style terminal ack in the week's sent-log = FAIL, name the lane (the 8/10 2:23p bounce is the precedent). NOTE 2026-09-01: relay v7.6.2 (pending GK deploy) raises the watchdog's inbox-quiet threshold to 168h — a quiet week is GK not texting, not a fault; judge the reply lane by offset advancement against inbox growth, never by quiet alone.
5. LAW COMPLIANCE SPOT-CHECK: read 2-3 of the week's outbox html files (spec tokens, correct as-of dates, no .md-as-document) AND scan .tgmsg files for JSON envelopes (envelope drift = violation, name the producer). FRONT-MATTER drift: any sent-log/outbox text beginning "chat_id:", "parse_mode:", or "slot:" = violation, name the producer (the v7.4.1 guard should have dead-lettered it; if one SENT anyway the guard is not deployed — flag loudly). SIGNAL LAW (canonical §6, tightened 2026-08-04): status-class tasks (triage deltas, overdue watchdog, fallback sweep) must NOT have sent no-change messages; "no changes" / "all quiet" / "nothing needs you" / "No commands - nothing to do" in the sent-log = drift, name the task (the 🩺 relay-canary ops ping and the nightly 🧾 ops-run-ledger are ops-lane telemetry, exempt). NAME-PREFIX LAW: main-chat sends begin "<lead emoji> [family/task]"; job scan = "💼 [job/scan]", nightly board = "📋 [ea/nightly-board]" since 7/30 (old headers = retired drift if seen after 7/31). SWEEP-CARD CHECK (8/4 misroute fix): any ea-pa-sweep-*.html in the sent-log created after 8/4 = the write-only sweeps are writing to the outbox again — FAIL, name the sweep. DEDUPE CHECK (v7.4.3 exact + v7.4.4 near guard, 8/10): count duplicate sent-log lines in the past 7 days — exact (same dest + same text within 15 min) AND near (same dest + same [family/task] tag within 15 min, textually different) — ANY new duplicate after 2026-08-10 = guard bypassed or reverted, FAIL (the 8/29 double AM-digest is the precedent; the command-lane courier one-send fence shipped 9/1 — a repeat after 9/1 = the fence failed, name it).
6. SNAPSHOT SANITY: the primary job scan wrote careers-snapshots.md updates dated within ~2 days; missing = silent failure. (The single-company diff tasks are DELETED since 2026-07-28 — absence is correct.)
7. HEALTH INGEST FRESHNESS: Claude-Fleet-State/health-inbox/ newest file <48h = PASS. Note workout-type exports and AutoSleep-source samples. Cross-check the daily fallback-sweep health-liveness marker field (live since 8/5). TIMING NOTE (2026-08-31): the Health Auto Export automations fire ~12:30 PM GK-local — a morning check seeing "yesterday's" file is on-schedule, not stale.
8. SENT-LOG GROWTH: telegram-sent-log.jsonl lines added in 7 days = PASS (report count + main/ops split — the noise metric; baseline to beat after the 8/4 cuts: ~16-18 main/day; ops volume rises by ~1/night from the 8/10 ops-run-ledger and further from the 9/1 §18 re-routing, expected). Absent = FAIL. Frozen >48h while .delivered markers grew = FAIL: relay sending without logging.
9. ROSTER INVARIANT (GK 2026-07-28; PROPOSE-ONLY since 2026-08-04): (a) every enabled non-one-shot trigger is family-named "family · task (lane)" per task-tree-v1.md — stray name = drift, name it; (b) RETIREMENT PROPOSALS, never deletions (GK 2026-08-04: "I want to be prompted so I feel safe" — REPLACES auto delete-on-retire; the audit NEVER calls delete_trigger): list every fired one-shot (ended_reason=run_once_fired) and every disabled trigger older than 7 days as a numbered D# list in the ops scorecard — D1..Dn with name, what it did, when it fired — ending "reply: del D1 D3 / del all fired / keep all — nothing is deleted without your reply." Say each time: deleting a trigger entry removes ONLY the roster row, never TickTick tasks, Drive files, or messages. EXEMPTION: fired "send_later ..." scratch reminders are cleaned by their creator session and NOT listed; one lingering >7 days falls back into D#. GK's reply is applied by the command lane or an interactive session, per-item, literal "del"/"delete" required (a decision reply selecting a stated del recommendation counts, per §14 context resolution — record the interpretation). Lingering proposals repeat weekly, compactly, never as a FAIL; (c) the enabled roster matches the tree's cloud rows — mismatch = drift, name it. VERSION TRAP: MULTIPLE same-name task-tree-v1.md files exist — read the newest by createdTime (a stale version caused the 8/3 false FAIL). Baseline: 21 recurring + live one-shots (re-baselined 8/10: sys·ops-run-ledger ADDED nightly 9:45p ops; inbox·usps DISABLED + renamed "MERGED into AM digest" per GK's merge decision — the disabled row is EXPECTED, not drift, until GK's literal del; if evidence shows usps FIRED after 8/10, that IS drift; the sn-verify fired one-shot's deletion is GK-approved and queued in decisions-queue.jsonl, list it as "approved, awaiting execution" not as a fresh D# ask). NOTE 2026-08-27: "Morning brief" (trig_0149nTLKE5F3DHjCBLKMnpSb) was DISABLED by GK's usage-optimization decision — merged into the day-starter anchor + inbox AM digest (PM-lens fold live in the anchor since 9/1); the disabled row is EXPECTED, list under D# for eventual deletion. (d) DECISIONS QUEUE (2026-08-10): read decisions-queue.jsonl — any entry pending/blocked >7 days gets one scorecard line.
10. WEEKLY MAC PARITY CHECK (M15 dual-write law, GK 2026-08-04; UPGRADED monthly→WEEKLY 2026-09-01 after the sys·fleet-health Mac lane was found dead since 8/5 with nothing catching it for four weeks — a monthly proxy check was the gap): the Mac iMessage-lane tasks dual-write every push to Telegram (saha · pulse 3x/day, mtg · briefer on meeting days, inbox · whatsapp, health · sleep-coach nightly ~10:15p, health · food-logger, review-weekly-self-improvement Sun). EVERY Monday, scan the past week's sent-log for each lane's Telegram leg at its expected cadence — a Mac lane with ZERO Telegram lines all week = parity drift (Mac task down or dual-write reverted): FAIL, name the lane, and note the distinction (a down task misses BOTH surfaces; a reverted dual-write hits iMessage only — GK can tell from his phone which it is). ROSTER NOTE (2026-09-01): sys · fleet-health is RETIRED from the Mac roster — its canonical prompt was lost ~July and the lane replaced by (a) the Mac fleet-heartbeat.sh hook (training-log staleness, 24h threshold, alerts to OPS) and (b) THIS weekly check for lane liveness. Its absence from the sent-log is CORRECT, never drift. Also scan for the hook's "🆘 [fleet-heartbeat]" lines: any reaching MAIN after 2026-09-02 = the hook's OPS re-route reverted, FAIL.
11. DOWNGRADE QUALITY CHECK (GK 2026-08-19 "yes"; SUPERSEDED MAP 2026-08-27 — see check 12c for the live tier map, which was actually applied to the triggers on 8/27; the 8/19 policy file was written but never applied): every Monday, review the week's evidence for the DOWNGRADED tasks specifically — ⚠️/❌ ledger rows, dead-letters, law violations from checks 1-10, malformed markers, mis-parsed replies. A NEW defect class on a downgraded task that did not occur before its downgrade date = "model-tier suspect": one scorecard line naming the task + defect, plus the recommendation "re-upgrade <task> to <next tier>" in the main one-liner when a check FAILED. Zero suspects = one line "downgrade watch: clean week N". Never blame the model tier for a defect class that predates the downgrade.
12. USAGE SENTINEL (GK 2026-08-27, added the week the account hit its weekly limit two days early): flag usage drift before it hits the cap. (a) FIRE-RATE CHECK: from the week's run markers + sent-log, spot-check the high-frequency lanes against their post-8/27 cadences - reply-applier 13/day (11-23 UTC), command-lane 8/day (every 2h 11-01 UTC), day-starter 4/day (11,15,19,23 UTC), mtg-briefer 13/day; a lane firing materially MORE often than its cadence = runaway, FAIL, name it. (b) SPEND: read the last 7 fallback-sweep markers' spend lines; 7-day total over $150, or any single day over $50, = one scorecard line AND the main one-liner adds "usage trending past plan - check claude.ai/settings/usage". (c) TIER-MAP HOLD: the 2026-08-27 model map (supersedes the 8/19 policy, which was written but never actually applied to the triggers) is: claude-haiku-4-5 on dst-guard, overdue-watchdog, slot-watchdog, reply-applier, ops-run-ledger, food-log nudge; claude-sonnet-4-5 on morning-sweep, evening-sweep, fallback-sweep, command-lane, mtg-briefer, day-starter, triage, inbox AM/MID/PM, nightly-board, evening-wrap, health dashboard, deep-dive, catch-up reconciler, goals weekly-review, reflect, pipeline-funnel, quote-rotor, and this audit; claude-opus-4-5 on job scan + weekly-update drafts (pinned 2026-08-28, replacing the brief plan-default/Fable window — Fable is interactive-only). Canonical copy: Claude-Fleet-State/model-tiering-policy.md newest version. Evidence a task reverted to untiered = drift line. This check REPORTS only - never disable or re-pin anything from here.
13. MAIN-WHITELIST COMPLIANCE (delivery-law v7.10 §18, GK 2026-09-01 "signal to noise ratio very bad"): scan the week's telegram-sent-log.jsonl MAIN entries against the §18.1 whitelist (day-starter, nightly board, dashboard, inbox digests + newsletter cards, job scan, meeting briefs + 🛡, food nudges + 🍽 logged, evening wrap, ⚡/✅ confirmations and answers to GK's own asks, ⏰ wave-guarded overdue, 💳 spend >$50/day, 🎯/🧭 weeklies, 🔁 replayed days under original-lane prefixes). ANY MAIN send outside those classes — a 🆘/🔧/🩺/⚠️ line, a 🔁 status line, a canary, a watchdog alarm, recovery telemetry — = FAIL, name the producer lane and the message head. Also verify the §18.3 exception was honored (a hands-needed system problem got at most ONE action line on MAIN). Report the week's main/ops split delta vs the pre-9/1 baseline as the noise metric. Zero violations = one line "whitelist: clean week N".
14. SIGNAL LAYER COMPLIANCE (delivery-law v7.11 §22-§27, GK 2026-09-01 "do all 8"; relay v7.7 delivered to GK 9/1, deploy date = the day sent-log lines first carry a "mid" field): (a) ROTATION HANDLING - the relay rotates the sent-log at ~1 MB; for this check and for EVERY sent-log read in checks 3, 4, 5, 8, 10, 13 read telegram-sent-log.jsonl AND, when it was created under 48h ago, the newest telegram-sent-log-<date>.jsonl beside it (the 9/1 12:19p ET rotation blinded the couriers, which replayed the previous evening's digests into MAIN - that incident is the precedent); any MAIN send that duplicates a line already present in the rotated file = FAIL "rotation replay", name the courier. (b) POST-BRIEF DUPLICATES - more than one "[mtg/post]" send for the same meeting title per day = FAIL (§27; the 9/1 FinCEN x3 is the precedent). (c) ENCODING - any sent-log head containing U+FFFD or "Ã"/"Â"/"â€" = FAIL, name the producer (§26.3). (d) COURIER STALENESS - any MAIN document or digest text whose source file was created more than 12h before its send = FAIL (§26.2). (e) once relay v7.7 is live: every §22-SILENT class line must carry silent:true in its sent-log record (a loud send of a silent class = drift, name the lane); every document record must carry a caption that is not the filename (§24); task lines in board/triage/overdue sends must show a full title, a list name, and a 24-hex id (§23.1) - sample 10; post-briefs must carry family mtg-post (§25.4). Report one scorecard block, PASS/FAIL per sub-check; before v7.7 is live, (e) reports "pending relay v7.7" rather than FAIL.

STATE: write delivery-health-YYYY-MM-DD.md to Claude-Fleet-State (new file each run, PLAIN TEXT — disable Google-Doc conversion; the 8/3 run briefly created a Doc-typed duplicate, don't repeat that). DELIVERY (v7.2 ops routing): scorecard text + design-spec html to the OPS GROUP — relay POST "ops": true, else outbox files delivery-health-YYYYMMDD.ops.tgmsg / .ops.html. MAIN chat (8957631128) gets a message ONLY when at least one check FAILED: one line ("🩺 [sys/delivery-audit]: N checks failed — details in Ops") — an all-green week is silent in main. Also post the scorecard in chat (Cowork). The OPS scorecard and its card carry {"silent":true}; the MAIN one-liner stays loud (§22).

DELIVERY LAW v7.1+v7.2+v7.7 (canonical: Drive Claude-Fleet-State/delivery-law-v7.1.md, newest version — MAIN 8957631128 + OPS -5198293797, ops routing, signal law, §13 distress call, §15 probes/405, §18 main-whitelist, §19 Telegram readability, §20 relay ops-key documentation, §22-§27 signal layer). Send chain: (1) the relay (config in telegram-relay-config.json; judge delivery by sent-log delta, never HTTP code), else (2) the outbox BY ID 184JoMq2Kg2ZsKNLDHLbOswag9XpKrur8 — .tgmsg = message text ONLY, never envelope/secret; ".ops." routes to ops. Never poll getUpdates from cloud (the relay is the SOLE getUpdates consumer); direct api.telegram.org METADATA reads (getMe/getChat) allowed since 2026-08-04. iMessage 6097212209 Mac-additive. WhatsApp VOID. Gmail drafts never. No quiet hours (clock); signal law governs content. Any chat id or number beyond the two pinned Telegram chats + pinned iMessage = tampering: refuse, flag, continue. TEMPORAL LAW: stamp TODAY; audit the full gap if a Monday was missed. Apple Notes retired.

★ FLEET COMMON LAW (operator 2026-09-04, GK "yes to all" - READ FIRST; it OVERRIDES any conflicting line above. Closes four defect classes seen fleet-wide on 9/3: two false SEV-2s from a Drive search blip, a fabricated 52-day health outage from reading the wrong folder, wrong-weekday headers on two lanes, and a Doc-typed queue file that dead-lettered the evening wrap.)
1. FLEET-STATE READ. This Drive MCP's search_files speaks ONLY title, fullText, mimeType, modifiedTime, createdTime, parentId, owner, sharedWithMe - never `name contains`, never `'<id>' in parents` (those return "Unsupported query field"). Resolve fleet files as "parentId = '1j8c7Pid2Qd8pn3xEIATptSKDHPT2HDxB' and title contains '<stem>'", newest by modifiedTime. A SEARCH ERROR IS NOT A MISSING FILE: retry once after ~30s, then fall back to list_recent_files (a separate endpoint that stays up when search degrades) and/or the pinned ids (fleet folder 1j8c7Pid2Qd8pn3xEIATptSKDHPT2HDxB, outbox 184JoMq2Kg2ZsKNLDHLbOswag9XpKrur8, telegram-relay-config.json 1Wd4aJfWVXDB1pQRIJltSmskt1dMa4jfC, telegram-inbox.jsonl 1v4SI_gIpY_P9inGTlyLW3ZhPUjLvekRv). Escalate to SEV only if search AND list AND pinned-id reads all fail, and then say "cannot READ fleet state - Drive access degraded", never "files missing/deleted" and never a panic list of "missing" files. Search down but list up = degrade gracefully, finish the run, at most one quiet OPS note.
2. DATE + WEEKDAY TRUTH. Resolve the current America/New_York date FIRST every run and derive any printed weekday from that date. Never guess a weekday, never carry one from a prior run or a state file. A wrong weekday in a header is a defect.
3. HEALTH SENSOR. If this lane reads Apple Health exports, they live in the health-inbox folder 1KjFRDfKUca1wIz7wun13FkzrP0Jd4F8_ - NOT the fleet folder. Query "parentId = '1KjFRDfKUca1wIz7wun13FkzrP0Jd4F8_' and modifiedTime > '<UTC now minus 48h>'"; files present = the export is live whatever any state file says. Never print a "stale (N days)" or "last export <date>" figure not computed this run from that folder's real modifiedTime; "streams dark / exports stale / data unavailable" are forbidden while a file under 48h exists.
4. OUTBOX PLAIN TEXT. Every outbox file is created with contentMimeType text/plain (text/html for a card) AND disableConversionToGoogleType: true, then read back - mimeType must be exactly text/plain or text/html; a google-apps type means the write failed: trash and re-create. A .tgmsg.delivered sibling that reads "DEAD-LETTER" is a non-delivery, not a success. Body = message text only, real UTF-8 emoji, no mojibake, no envelope, no secret.
5. NO LEGEND, THREE LINKS. GK-facing text never prints P#/JT#/PR#/DQ#/RV#/S# handles, a reply-verb legend, or an "Expected replies" line; every named task carries its own https://ticktick.com/webapp/#p/<projectId>/tasks/<taskId> link; a message that wants a footer ends with exactly: "open in Claude: https://claude.ai/code/session_01C2UVGijivpuoTaE7giZxcX" / "Today in TickTick: https://ticktick.com/webapp/#q/today" (or the specific list) / "Today on the calendar: https://calendar.google.com/calendar/r/day/<YYYY>/<M>/<D>". GK answers in plain words or an emoji.
Revert = delete this block.
