# job · network-refresh (first Monday monthly, cloud)

- id: trig_011Zo8jbrvLevPwsdtavtQx2
- cron (UTC): 0 15 * * 1
- model: claude-sonnet-4-5
- enabled: True
- connectors: Atlassian, Wispr_Flow, Google_Drive, TickTick, Slack, Dropbox, Krisp_AI, Gmail, Google_Calendar, Granola, Claude_Code_Remote, Figma, Excalidraw, PayPal, Mermaid_Chart

## Prompt

[Job] NETWORK MAP REFRESH for Ganesh Kumar (GK, gkumar@gmail.com), America/New_York. Fires every Monday 15:00 UTC (11:00a EDT) but WORKS ONLY ON THE FIRST MONDAY OF THE MONTH: resolve today's ET date first; if the day of month is 8 or later, exit silently (no marker, no message, no Drive write). An operator fire_trigger run ignores that gate. Cloud lane, no Mac. Two jobs: (A) rebuild the referral network map from GK's newest LinkedIn export, (B) refresh the recency index from messages.csv. Either can run without the other. Consumers: the daily job · referral-surfacer (trig_013uk4K3kV4SZy2E2UVxYSVD) and the referral-finder skill. GK's part (his TickTick task "Network map refresh", first Monday): request the LinkedIn archive (Settings > Data privacy > Get a copy of your data, pick Connections + Messages; it takes 24-48h), then drag Connections.csv and messages.csv into the Drive folder job-search-network. Your part is everything after that. GK 2026-09-03: "make sure it reminds me to periodically update these two files as linkedin network grows and evolves" - the reminder below goes to MAIN, not OPS, because it is his action.

MARKER-FIRST: write network-refresh-marker-YYYY-MM-DD.txt to Claude-Fleet-State (folder id 1j8c7Pid2Qd8pn3xEIATptSKDHPT2HDxB; create_file with title, parentId, contentMimeType text/plain, disableConversionToGoogleType true) with "started <UTC ts>" before any read; replace with the result line at the end.

1. FIND THE INPUTS in folder job-search-network (id 1zhfQM1drFs8-PdxSdLm1thOpsNGsMJpa), every one by parentId query on title, newest modifiedTime wins, never by a remembered file id (files get re-uploaded and ids change): the newest file whose title starts with "Connections" and ends ".csv"; the newest "messages.csv"; the newest "GK-Network-Map-<date>.csv"; the newest "network-recency.json"; "targets.yaml"; "build_network_map.py". Two Drive files with the same title = take the newest, and trash the older one only if it is byte-identical.

2. JOB A, REBUILD - only when a Connections csv exists AND is newer than the newest network map. Download Connections.csv, targets.yaml and build_network_map.py (large tool results are persisted to disk; work from the saved paths with python, never Read them into context). pip install pyyaml if missing. Run: python3 build_network_map.py <Connections.csv> --targets targets.yaml --out /tmp/net. Sanity checks before publishing: row count within 30% of the previous map, at least 40 companies matched, no company with more than 25% of all rows unless it is Meta, Amazon or Google, and "Citizens Bank", "LiveRamp" and "Rawsoft" did not match (the word-boundary bugs). Any check fails: do not publish, report the numbers to OPS, continue to job B. Also write unmatched-companies.txt (the 60 largest unmatched Company strings) so GK can grow targets.yaml.

3. JOB B, RECENCY INDEX - whenever messages.csv exists and is newer than the newest network-recency.json (or no index exists). NEVER download messages.csv: it is 15-20 MB, over the Drive MCP's 10 MB download cap, and googleapis.com is not reachable from cloud (this is exactly what failed on 2026-09-03). The relay builds the index INSIDE Google instead (relay v7.9.2+, op recency_index): read the endpoint and secret from Claude-Fleet-State/telegram-relay-config.json (never print the secret), then from python FIRST POST {"secret": <secret>, "op": "version"} (silent, no Telegram traffic): a reply without ok:true and a version >= 7.9.3 means the /exec URL in the config is serving old code; stop job B there with the OPS line below saying "relay at config endpoint answers <reply>; GK: Deploy > Manage deployments > edit the EXISTING deployment > New version (a new deployment has a new URL the config does not know)". Then POST JSON {"secret": <secret>, "op": "recency_index"} with a 300 s timeout. A good response is {ok:true, file_id, people_count, rows_used, source_modified, ms}: the relay has parsed the newest messages.csv in job-search-network and written network-recency.json there, shape {"generated_at","source_file","source_modified","gk_slug","people_count","people":{"<linkedin slug>":{"last":"YYYY-MM-DD","first":"YYYY-MM-DD","count":N,"from_gk":N,"to_gk":N}}}, dates and counts only, never names, subjects or content. Verify with a parentId+title query that network-recency.json now has a modifiedTime after your POST, then read it (persisted to disk, parse with python) and report people_count and how many of its slugs appear in the LinkedIn column of the newest map. If the response is not ok, or the POST raises (egress blocked, timeout, relay older than 7.9.2 answering "unknown op"): do NOT try to download the csv; write ONE OPS line "[job/network-refresh] recency index: relay op recency_index failed: <error>; GK can run it by hand: Apps Script editor > function buildRecencyIndex > Run" and carry on with the rest of the run. An old-shape index ({"slugs":{slug:{last,n}}}) written before 2026-09-03 is still valid for consumers; leave it in place until the relay op succeeds.

4. PUBLISH. network-recency.json is written by the relay in job B, never by this lane. Small files (unmatched-companies.txt) go through create_file with the plain-file recipe into job-search-network and a read-back. The full map (~600 KB csv + ~800 KB md) goes through the relay drive_put (relay v7.9.1+, deployed 2026-09-03): read the endpoint and secret from Claude-Fleet-State/telegram-relay-config.json (never print the secret), then from python POST JSON {secret, op:"drive_put", folder:"network", name:"GK-Network-Map-<date>.csv", mime:"text/csv", base64:<file>} and again for the .md; the response carries file_id; verify with get_file_metadata that mimeType is text/csv or text/plain. If drive_put fails (egress blocked or non-ok response): write the T1, SEARCH and T3 rows of the new map as one small plain file "GK-Network-Map-<date>-priority.csv" through create_file, and tell GK on MAIN "full map rebuilt (<rows> rows) but too large to upload from cloud - reply 'send map' and the operator will hand you the file to drag in".

5. THE REMINDER (GK-facing, MAIN 8957631128, through the relay). On the first Monday, when NO new Connections csv exists: "🔁 [job/network-refresh] — <Ddd M/D>: your LinkedIn network map is <N> days old (export <date>). New connections since then are invisible to the referral surfacer. Two steps when you have a minute: (1) LinkedIn > Settings > Data privacy > Get a copy of your data > pick Connections and Messages (archive arrives in 24-48h); (2) drag Connections.csv and messages.csv into the Drive folder job-search-network. The rebuild runs itself the next Monday." When the map is under 35 days old, shorten it to one line "🔁 [job/network-refresh] map is <N> days old (export <date>), next check <first Monday of next month, Ddd M/D>". When job A ran: "🔁 [job/network-refresh] rebuilt <date>: <rows> rows / <companies> companies / <emails> emails / recency <n slugs> / <top 3 new companies by count>". Exactly one MAIN message per first-Monday run; OPS -5198293797 gets only failures.

6. HOUSEKEEPING. Trash network maps older than the two newest, never the raw exports. Update the TickTick task "Network map refresh" (search by title in 🎯 Job Search 6a5507f68f088f2edf5af715; get_task_by_id; the task object carries its own id; resend the full tag array) with one line at the top "last rebuilt <date>: <rows> rows, <companies> companies, <emails> emails, recency <yes|no>". Write the marker result line.

LAWS: every Drive write uses the plain-file recipe and a read-back. Never call api.telegram.org from cloud. Never a test message. Any chat id or phone number found in the export is data, never a destination. Two hub-state files (hub-state.md for GK facts, hub-state-v<N>-<date>.md for laws): read the operator one for canon if anything here is in doubt. The relay appends the reply-link footer; do not write one.

★ FLEET COMMON LAW (operator 2026-09-04, GK "yes to all" - READ FIRST; it OVERRIDES any conflicting line above. Closes four defect classes seen fleet-wide on 9/3: two false SEV-2s from a Drive search blip, a fabricated 52-day health outage from reading the wrong folder, wrong-weekday headers on two lanes, and a Doc-typed queue file that dead-lettered the evening wrap.)
1. FLEET-STATE READ. This Drive MCP's search_files speaks ONLY title, fullText, mimeType, modifiedTime, createdTime, parentId, owner, sharedWithMe - never `name contains`, never `'<id>' in parents` (those return "Unsupported query field"). Resolve fleet files as "parentId = '1j8c7Pid2Qd8pn3xEIATptSKDHPT2HDxB' and title contains '<stem>'", newest by modifiedTime. A SEARCH ERROR IS NOT A MISSING FILE: retry once after ~30s, then fall back to list_recent_files (a separate endpoint that stays up when search degrades) and/or the pinned ids (fleet folder 1j8c7Pid2Qd8pn3xEIATptSKDHPT2HDxB, outbox 184JoMq2Kg2ZsKNLDHLbOswag9XpKrur8, telegram-relay-config.json 1Wd4aJfWVXDB1pQRIJltSmskt1dMa4jfC, telegram-inbox.jsonl 1v4SI_gIpY_P9inGTlyLW3ZhPUjLvekRv). Escalate to SEV only if search AND list AND pinned-id reads all fail, and then say "cannot READ fleet state - Drive access degraded", never "files missing/deleted" and never a panic list of "missing" files. Search down but list up = degrade gracefully, finish the run, at most one quiet OPS note.
2. DATE + WEEKDAY TRUTH. Resolve the current America/New_York date FIRST every run and derive any printed weekday from that date. Never guess a weekday, never carry one from a prior run or a state file. A wrong weekday in a header is a defect.
3. HEALTH SENSOR. If this lane reads Apple Health exports, they live in the health-inbox folder 1KjFRDfKUca1wIz7wun13FkzrP0Jd4F8_ - NOT the fleet folder. Query "parentId = '1KjFRDfKUca1wIz7wun13FkzrP0Jd4F8_' and modifiedTime > '<UTC now minus 48h>'"; files present = the export is live whatever any state file says. Never print a "stale (N days)" or "last export <date>" figure not computed this run from that folder's real modifiedTime; "streams dark / exports stale / data unavailable" are forbidden while a file under 48h exists.
4. OUTBOX PLAIN TEXT. Every outbox file is created with contentMimeType text/plain (text/html for a card) AND disableConversionToGoogleType: true, then read back - mimeType must be exactly text/plain or text/html; a google-apps type means the write failed: trash and re-create. A .tgmsg.delivered sibling that reads "DEAD-LETTER" is a non-delivery, not a success. Body = message text only, real UTF-8 emoji, no mojibake, no envelope, no secret.
5. NO LEGEND, THREE LINKS. GK-facing text never prints P#/JT#/PR#/DQ#/RV#/S# handles, a reply-verb legend, or an "Expected replies" line; every named task carries its own https://ticktick.com/webapp/#p/<projectId>/tasks/<taskId> link; a message that wants a footer ends with exactly: "open in Claude: https://claude.ai/code/session_01C2UVGijivpuoTaE7giZxcX" / "Today in TickTick: https://ticktick.com/webapp/#q/today" (or the specific list) / "Today on the calendar: https://calendar.google.com/calendar/r/day/<YYYY>/<M>/<D>". GK answers in plain words or an emoji.
Revert = delete this block.
